<% @bookings.each do |booking| %>
  <% if booking.end_date < Date.parse(Time.now.strftime('%Y/%m/%d'))%>
    <% if booking.status == "confirmed" %>
      <div class="card">
        <div class="card-header">
          <h6 class="my-0" style= "height: 16px;"> Status: <%=booking.status%></h6>
        </div>
      </div>
    <% end %>
  <% end %>
<% end %>
